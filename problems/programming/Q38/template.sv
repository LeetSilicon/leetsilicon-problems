class Solution:
  def strStr(self, text: str, pattern: str) -> int:
    # TODO
    return -1

if __name__ == "__main__":
  sol = Solution()

  text = input().strip()
  pattern = input().strip()
  print(sol.strStr(text, pattern))
