class Solution:
  def isSubsequence(self, s: str, t: str) -> bool:
    # TODO
    return False

if __name__ == "__main__":
  sol = Solution()

  s = input().strip()
  t = input().strip()
  print(sol.isSubsequence(s, t))
